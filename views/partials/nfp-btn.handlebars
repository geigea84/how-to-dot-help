        <div class="content">
            <div class="card2 flex-row">
                <div class="col-3">
                    <img src="/images/filler-logo.png">
                </div>
                <div class="bod col-6">
                <h2>{{dataValues.nfp_name}}: {{dataValues.cause}}</h2>
                <h4>Tags: {{dataValues.tags}}</h4>
                <h4>Location: {{city}}, {{state}}</b></h4>
                <p>About this NFP: {{dataValues.description}}</p>
                        <button class="reach-out" type="button">Reach Out</button>
                </div>
            </div>
        </div>